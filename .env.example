# Payloadify currently ships as a fully static, client-side site — no environment
# variables are read anywhere in the codebase (verified: no `process.env` usage).
#
# This file is kept as a placeholder for when a backend becomes unavoidable
# (e.g. a Cloudflare Worker for DNS lookups, per CLAUDE.md's build notes).
# Add real variable names here (without values) once one is introduced.
