{
  "name": "@payloadify/cvss-core",
  "version": "1.0.0",
  "description": "Shared CVSS 3.1/4.0 scoring engine and vulnerability reference/template data used by Payloadify's web and desktop tools.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
